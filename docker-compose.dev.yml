services:
  file-service:
    # 开发用覆盖：挂载源码并启用 reload，不修改 base 的端口/名称
    volumes:
      - .:/app
      - ./file_records.db:/app/file_records.db
    command: uvicorn main:app --host 0.0.0.0 --port 8808 --reload
    restart: "no"
